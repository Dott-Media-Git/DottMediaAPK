name: Audit suspicious Firebase auth users

on:
  workflow_dispatch:
    inputs:
      delete_confirmed:
        description: Delete the exact allowlisted test accounts after auditing
        required: false
        type: boolean
        default: false

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: backend/package-lock.json
      - run: npm ci --no-audit --no-fund
      - name: List matching auth records
        env:
          FIREBASE_SERVICE_ACCOUNT_JSON: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_JSON }}
          DELETE_CONFIRMED_TEST_USERS: ${{ inputs.delete_confirmed }}
        run: node scripts/auditSuspiciousAuthUsers.mjs
